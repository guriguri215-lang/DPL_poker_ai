# configs/opponents/training

Opponent model configs for the **training** split (leak amounts {0.08, 0.16}
plus GTO). Used to tune detection thresholds and alpha. Placeholder until
Phase 6. Splits are physically separated so evaluation code cannot peek at
validation/test (REV-20260702 5.3).
