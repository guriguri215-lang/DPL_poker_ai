# configs/opponents/training

Training contains a GTO negative control and seen single-leak types at deltas
0.08 and 0.16. Each JSON file records the stable opponent ID, equilibrium
version and frozen-artifact digest, generation method, split, allocation, and
seed.

The P6-3 catalog does not tune thresholds or alpha; it only provides reproducible
model inputs for later Training work.
