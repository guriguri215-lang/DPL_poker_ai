# experiments

Reserved package for a future high-level experiment API. It is still a
placeholder and should not be imported as an implemented runner.

The current manifest-driven Training, Validation, calibration, and Gate B
implementations live in [`src/phase6`](../phase6/), while the smallest runnable
session is [`cli/run_session.py`](../../cli/run_session.py).
